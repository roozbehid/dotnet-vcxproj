This example solution contain a C# project file dynamiclly linking to .dll or .so project in C++.

You should be able to compile and run it in both windows and linux with just issuing

     dotnet build